import random   # 乱数発生に必要なモジュールを読み込みます。
def
over1 = list(range(1,76))
over2 = random.choice(over1)
over3 = over1.remove(over2)
return 

# while True:
#     val = input()
#     for over2 in over1:
#         over1.remove(over2)
            
#     else:
#         print(over1,over2)
            
    
